import type { Config } from "prettier";

const config: Config = {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 120,
  useTabs: false,
  tabWidth: 2,
};

export default config;
